
function whosPlaying(names) {
    var playername = ["Angela", "Kaung Pyae", "James"];
    var randomplayer = playername[Math.floor(Math.random()*playername.length)];
    return randomplayer + " is going to buy lunch today";
}
